<?xml version="1.0" encoding="utf-8"?>
<mx:Panel xmlns:mx="http://www.adobe.com/2006/mxml" layout="absolute" width="779" height="466" title="Eliminar Pre-operatorio" borderColor="#15AD8F">
		<mx:Script>
		<![CDATA[
			import mx.controls.List;
			import mx.events.ListEvent; 			
			import mx.controls.Alert;
			import mx.collections.ArrayCollection;
            import mx.controls.CheckBox;    
            import mx.events.CloseEvent;
            import mx.rpc.events.ResultEvent;
			
			import transferObjects.UsuarioElim;
			import mx.rpc.events.ResultEvent;
			
	        [Bindable]
			private var preoperatorio1:ArrayCollection;
			
			<!--
			 * Desplega un mensaje de alerta al usuario del sistema si desea eliminar 
			 * un preoperatorio de la mascota realizado al interior de la clinica veterinaria.
			 * el cual da las opciones de Yes: para la eliminación de el o los preoperatorios seleccionados
			 * o NO: si no desea eliminar ningun preoperatorio seleccionado 
			 -->
			
			private function deleteRow():void {
			 Alert.show("Está seguro que desea eliminar Preoperatorio ","Confirme eliminación ", Alert.YES | Alert.NO, this, delRowHandler, null, Alert.NO);
			}
			
			<!--
			 * Señala el evento seleccionado por el usuario al momento de confirmar la eliminación de 
			 * un preopretorio.
			 -->
			
			private function delRowHandler(evt:CloseEvent):void {
			 if ((evt.detail == Alert.NO) || (evt.detail == Alert.CANCEL)) 
			 	return;
			  //removeTaskRecord();
			}
			
			<!--
			 * selecciona a todos los preoperatorios realizado a la mascota
			 * al interior del pabellón de la clinica veterinaria.
			 -->
	
			
			private function selectAll():void{
               /* var allRows:int = preopretorio.length;
                for (var i:int = 0; i < allRows; i++){
                    if (todo.selected)
                        persons[i].sel = true;
                    else
                        persons[i].sel = false;
                }
                tablaUsuarios.dataProvider = persons;*/
            }
			
	 	]]>
	   </mx:Script>
	    
	<mx:DataGrid x="10" y="74" width="739" height="206" id="tablaPreoperatorio" dataProvider="{preoperatorio1}" >
		<mx:columns>
			<mx:DataGridColumn width="30" headerText="" dataField="checkBox" id="checkBox" >
			 	<mx:itemRenderer>
			 	                <mx:Component>
			 	                    <mx:CheckBox click="data.sel=!data.sel"  selected="{data.selall}"/>            
			 	                </mx:Component>
			 	     	</mx:itemRenderer>
			      </mx:DataGridColumn>
			<mx:DataGridColumn width="160" headerText="Observaciones" dataField="observacionesPre"/>
			<mx:DataGridColumn width="160" headerText="Sintomas" dataField="sintomaPre"/>
			<mx:DataGridColumn width="160" headerText="Diagnostico" dataField="diagnosticoPre"/>			
			<mx:DataGridColumn width="90" headerText="Servicio" dataField="servicio"/>
			<mx:DataGridColumn width="60" headerText="Hora" dataField="hora"/>
			<mx:DataGridColumn width="60" headerText="Fecha" dataField="fecha"/>
			</mx:columns>
	</mx:DataGrid>
	
	<!--eliminación del preoperatorio-->
	<mx:Button x="523" y="302" label="Eliminar" id="eliminarPreoperatorio" click="{deleteRow()}" toolTip="Elimina el o los usuarios seleccionados" width="102" height="23" cornerRadius="6"/>
	
	<!--Cancela la eliminación del preoperatorio-->
	<mx:Button x="643" y="302" label="Cancelar" id="cancelarPreoperatorio" toolTip="Cancela la operación" cornerRadius="6" width="102" height="23"/>
	
	<!--CheckBox que seleciona los preoperatorios que se desea eliminar-->
	<mx:CheckBox x="10" y="48" id="selall" click="{selectAll()}"/>
	<mx:Label x="32" y="48" text="Seleccionar todo"/>
	
	<!--Realizacion de la Busquedad-->
	<mx:TextInput x="59" y="10" id="search"/>
	<mx:Label x="10" y="12" text="Buscar"/>
	
</mx:Panel>
